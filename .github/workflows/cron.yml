name: EDGAR Ingestion Pipeline

on:
  schedule:
    - cron: '0 11 * * *'
  workflow_dispatch:

jobs:
  ingest:
    runs-on: ubuntu-latest

    steps:
      - name: Batch 0
        run: |
          curl -f -X GET "${{ secrets.VERCEL_URL }}/api/insider/ingest?batch=0&start_date=2026-04-27&end_date=2026-05-02" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"

      - name: Wait 5 minutes
        run: sleep 300

      - name: Batch 1
        run: |
          curl -f -X GET "${{ secrets.VERCEL_URL }}/api/insider/ingest?batch=1&start_date=2026-04-27&end_date=2026-05-02" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"

      - name: Wait 5 minutes
        run: sleep 300

      - name: Batch 2
        run: |
          curl -f -X GET "${{ secrets.VERCEL_URL }}/api/insider/ingest?batch=2&start_date=2026-04-27&end_date=2026-05-02" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"

      - name: Wait 5 minutes
        run: sleep 300

      - name: Batch 3 + Price Enrich
        run: |
          curl -f -X GET "${{ secrets.VERCEL_URL }}/api/insider/ingest?batch=3&start_date=2026-04-27&end_date=2026-05-02" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" &
          curl -f -X GET "${{ secrets.VERCEL_URL }}/api/insider/price-enrich" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" &
          wait

      - name: Wait 5 minutes
        run: sleep 300

      - name: Score
        run: |
          curl -f -X GET "${{ secrets.VERCEL_URL }}/api/insider/score" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
